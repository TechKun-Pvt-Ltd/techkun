// Below this viewport width, the nav's contact options move from the header into the bottom nav.
const NAV_CONTACT_OPTIONS_BREAKPOINT_PX = 560;
export const NAV_CONTACT_OPTIONS_NARROW_QUERY = `(max-width: ${NAV_CONTACT_OPTIONS_BREAKPOINT_PX}px)`;